apiVersion: v1
kind: Service
metadata:
  name: api-cliente-service
spec:
  type: NodePort
  selector:
    app: api-cliente
    version: blue
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
